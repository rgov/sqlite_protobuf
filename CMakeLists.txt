cmake_minimum_required(VERSION 3.2)

# Search for modules in the cmake/ directory
set(CMAKE_MODULE_PATH
    ${CMAKE_MODULE_PATH}
    "${CMAKE_CURRENT_SOURCE_DIR}/cmake"
)

add_subdirectory(src)
add_subdirectory(example)

enable_testing()
add_subdirectory(tests)
